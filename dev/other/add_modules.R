add_modules <- function(data, id, metadata, dataset_info, link) {
  
  add_row(data,
          id = id,
          metadata = metadata,
          dataset_info = dataset_info,
          link = link)
  
}